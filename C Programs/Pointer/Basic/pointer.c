#include<stdio.h>
void main()
{int a;
 int *ptr;
 a=12;
 printf(" the numbers is %d\n",a);
 ptr=&a;
 printf(" the hexadecimal value of a is %x\n",a);
 printf(" address of a is %p \n",&a);
 printf(" value of variable pointed by the pointer is %d\n",*ptr);
 printf(" value in the pointer is %p \n",ptr);
 printf(" address of pointer is %p \n",&ptr);
 printf(: increment
}
